language: python
sudo: false

addons:
    apt:
        packages:
            - libhdf5-serial-dev

python:
  - "2.7"
  - "3.3"
  - "3.6"

install:
  - pip install numpy
  - pip install coveralls==0.5
  - pip install -e .\[tests\]

script: py.test -v --cov psyrun

after_success: coveralls
